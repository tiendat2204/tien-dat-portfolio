---
title: "StickerCop - React Sprite Sheet Animation Component"
publishedAt: "2025-06-12"
summary: "A reusable React component for creating Facebook-style interactive sticker animations using sprite sheets"
image: "/stickers/hero-image.png"
author: "Tien Dat"
tags: ["react", "animation", "ui", "frontend", "sprite-sheets"]
---

import { Preview } from '@/components/docs/preview'
import { CodeBlock } from '@/components/docs/codeblock'
import { InstallTabs } from '@/components/docs/install-tabs'

<Image
  src="/stickers/sprite-sheet-example.png"
  alt="Example of a sprite sheet with multiple animation frames"
  width={800}
  height={400}
  className="rounded-lg shadow-md"
/>

# StickerCop

A lightweight React component for creating interactive sprite sheet animations similar to Facebook stickers.

---

## 🖼️ Sticker Demo

Hover over a sticker to see it animate, and click to select it!

<Preview background="grid" className="mb-4">
  <div className="flex gap-4 flex-wrap justify-center">
    <StickerCop
      id="cute-cat"
      frameCount={16}
      frameRate={12}
      framesPerRow={4}
      framesPerCol={4}
      spriteImg="/stickers/cat-sprite.png"
      frameSize={80}
      onStickerClick={(id) => alert(`You clicked the ${id} sticker!`)}
    />
    <StickerCop
      id="thumbs-up"
      frameCount={24}
      frameRate={15}
      framesPerRow={6}
      framesPerCol={4}
      spriteImg="/stickers/sprite-2.png"
      frameSize={80}
      onStickerClick={(id) => alert(`You clicked the ${id} sticker!`)}
    />
  </div>
</Preview>

👉 [See Full Demo](https://sticker-animation-umber.vercel.app/)

---

## Installation

<InstallTabs
  npm="npm install @tiendat/sticker-cop"
  yarn="yarn add @tiendat/sticker-cop"
  pnpm="pnpm add @tiendat/sticker-cop"
  bun="bun add @tiendat/sticker-cop"
/>

## Usage

<CodeBlock
  language="tsx"
  fileName="components/example.tsx"
  code={`import StickerCop from '@/components/ui/sticker-cop'

export default function Example() {
  const handleStickerClick = (id: string) => {
    console.log(\`Sticker \${id} was clicked!\`)
    // Add your logic here
  }

  return (
    <div className="flex gap-4">
      <StickerCop
        id="cute-cat"
        frameCount={16}
        frameRate={12}
        framesPerRow={4}
        framesPerCol={4}
        spriteImg="/stickers/cat-sprite.png"
        frameSize={80}
        onStickerClick={handleStickerClick}
      />

      <StickerCop
        id="thumbs-up"
        frameCount={24}
        frameRate={15}
        framesPerRow={6}
        framesPerCol={4}
        spriteImg="/stickers/sprite-2.png"
        frameSize={80}
        onStickerClick={handleStickerClick}
      />
    </div>
  )
}`}
/>

## Props

| Prop | Type | Description |
| --- | --- | --- |
| `id` | `string` | Unique identifier for the sticker |
| `frameCount` | `number` | Total number of animation frames |
| `frameRate` | `number` | Speed of animation in frames per second |
| `framesPerCol` | `number` | Number of frames vertically in the sprite sheet |
| `framesPerRow` | `number` | Number of frames horizontally in the sprite sheet |
| `spriteImg` | `string` | URL to the sprite sheet image |
| `className` | `string?` | Optional additional CSS classes |
| `frameSize` | `number?` | Size of each individual frame (default: 64px) |
| `onStickerClick` | `(id: string) => void` | Callback when sticker is clicked |

## Features

- ✅ Play animations on hover
- ✅ Support for various sprite sheet layouts
- ✅ Configurable frame rates
- ✅ Customizable sizes
- ✅ React/Next.js friendly
- ✅ Performance optimized with useMemo/useCallback
- ✅ TypeScript support

## Example: Sticker Picker

Here's an example of how to implement a sticker picker component:

<Preview background="gray">
  <StickerDemo />
</Preview>

<CodeBlock
  language="tsx"
  fileName="components/sticker-demo.tsx"
  code={`'use client'
import React, { useState } from 'react'
import StickerCop from '@/components/ui/sticker-cop'

// Sample sticker data
const STICKERS = [
  {
    id: 'thumbs-up',
    frameCount: 20,
    frameRate: 15,
    framesPerRow: 5,
    framesPerCol: 4,
    spriteImg: '/stickers/thumbs-up-sprite.png',
    label: 'Thumbs Up'
  },
  {
    id: 'heart',
    frameCount: 24,
    frameRate: 18,
    framesPerRow: 6,
    framesPerCol: 4,
    spriteImg: '/stickers/heart-sprite.png',
    label: 'Heart'
  },
  // Add more stickers as needed
]

const StickerDemo = () => {
  const [selectedSticker, setSelectedSticker] = useState<string | null>(null)

  const handleStickerClick = (id: string) => {
    setSelectedSticker(id)
    // In a real app, you might send this sticker to a chat
    console.log(\`Selected sticker: \${id}\`)
  }

  return (
    <div className="w-full">
      {selectedSticker && (
        <div className="mb-6 p-3 text-center bg-indigo-50 dark:bg-indigo-900/30 rounded-lg">
          <p className="text-sm font-medium">
            You selected: <span className="text-indigo-600 dark:text-indigo-400">{selectedSticker}</span>
          </p>
        </div>
      )}

      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        {STICKERS.map((sticker) => (
          <div
            key={sticker.id}
            className={\`p-4 flex flex-col items-center \${
              selectedSticker === sticker.id
                ? 'bg-blue-100 dark:bg-blue-900/40 ring-2 ring-blue-400 dark:ring-blue-500'
                : 'bg-white dark:bg-gray-800'
            } rounded-lg transition-all\`}
          >
            <StickerCop
              id={sticker.id}
              frameCount={sticker.frameCount}
              frameRate={sticker.frameRate}
              framesPerRow={sticker.framesPerRow}
              framesPerCol={sticker.framesPerCol}
              spriteImg={sticker.spriteImg}
              frameSize={80}
              onStickerClick={handleStickerClick}
            />
            <span className="mt-2 text-sm font-medium">{sticker.label}</span>
          </div>
        ))}
      </div>
    </div>
  )
}

export default StickerDemo`}
/>
